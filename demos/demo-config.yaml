proxy:
  listen_addr: "127.0.0.1:18687"
  upstream_base_url: "https://api.openai.com"
  log_paths:
    - "/v1/chat/completions"
    - "/v1/completions"

management:
  listen_addr: "127.0.0.1:18677"
  auth:
    username: "demo"
    password: "demo1234"

database:
  path: "./demos/demo.db"

logging:
  level: "warn"
