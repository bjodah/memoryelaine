proxy:
  listen_addr: "0.0.0.0:8687"
  upstream_base_url: "https://api.openai.com"
  timeout_minutes: 23
  log_paths:
    - "/v1/chat/completions"
    - "/v1/completions"

management:
  listen_addr: "0.0.0.0:8677"
  auth:
    username: "admin"
    password: "changeme"
  preview_bytes: 65536  # 64KB — max body bytes returned inline

database:
  path: "./memoryelaine.db"

logging:
  max_capture_bytes: 8388608  # 8MB
  level: "info"
