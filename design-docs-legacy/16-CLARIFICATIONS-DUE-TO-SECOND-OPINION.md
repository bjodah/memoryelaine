1. Q: FTS, how does full text search happen with respect to streamed answers today? I hope it's being indexed on the stitched-together answer? If not we would probably need to add this as a bullet on our to-fix-list.
2. I wonder if the full text Search is based on stitch together complete, flowing text answers or if it's impaired by the streaming server event's chunks. If that is the case we should add to the to -do list to make sure that full text search uses the reconstructed form messages As for backward compatibility it is a -issue for this software. we are free to rewrite it from scratch we have no dependent uses and we can throw away the old log databases at this point in time. The second opinion makes a case that deduplication might not be needed or is overly complicated. I'm unsure if this is a reasonable stance. if the code storage of service -side streamed events is non -deterministic. and we need to store the stitched together final results in order for the deduplication to work. we could then make it an opt -in in the configuration variable, not to store the raw original data, but to store the reconstructed stream that is stitched into one single continuous answer. when the user then opts in to using this, the logged entries in the the database would make no distinction between streamed and non -streamed responses. This might be beneficial if this then would allow for us to deduplicate the messages in long chains of conversations.
